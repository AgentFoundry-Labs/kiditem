---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - package.json
autonomous: true
requirements:
  - SCHM-01
  - SCHM-02
  - SCHM-03
  - SCHM-04

must_haves:
  truths:
    - "CoupangOrder + CoupangOrderItem 2-tier 모델이 prisma/schema.prisma에 존재한다"
    - "CoupangReturn + CoupangReturnItem 2-tier 모델이 prisma/schema.prisma에 존재한다"
    - "Product 모델에 deliveryInfo, images 필드가 추가되고 ProductItem 모델이 존재한다"
    - "모든 쿠팡 대형 ID 필드가 String @db.VarChar(30) 타입이다"
    - "npm run db:push가 성공하고 npx prisma generate가 완료된다"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "CoupangOrder, CoupangOrderItem, CoupangReturn, CoupangReturnItem, ProductItem 모델 + Product 확장"
      contains: "model CoupangOrder"
    - path: "package.json"
      provides: "db:seed-coupang npm script"
      contains: "db:seed-coupang"
  key_links:
    - from: "prisma/schema.prisma"
      to: "PostgreSQL"
      via: "npm run db:push"
      pattern: "@@map.*coupang_orders"
---

<objective>
Prisma 스키마에 쿠팡 주문/반품/상품 모델을 추가하고 DB에 적용한다.

Purpose: 이후 서비스 수정(Plan 02)과 데이터 임포트(Plan 03)의 전제 조건이 되는 스키마 기반을 확립한다.
Output: 업데이트된 prisma/schema.prisma, Prisma Client 재생성, DB 테이블 생성
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@prisma/schema.prisma
</context>

<interfaces>
<!-- Research에서 확인된 스키마 설계. 아래 패턴 그대로 구현. -->

From 01-RESEARCH.md Pattern 1 (CoupangOrder):
```prisma
model CoupangOrder {
  id            String   @id @default(uuid()) @db.Uuid
  companyId     String   @map("company_id") @db.Uuid
  shipmentBoxId String   @unique @map("shipment_box_id") @db.VarChar(30)
  orderId       String   @map("order_id") @db.VarChar(30)
  orderer       Json?
  receiver      Json?
  status        String   @default("ACCEPT")
  deliveryCompanyName String?  @map("delivery_company_name")
  invoiceNumber String?  @map("invoice_number")
  parcelPrintMessage  String?  @map("parcel_print_message")
  shippingPrice Int      @default(0) @map("shipping_price")
  totalPrice    Int      @default(0) @map("total_price")
  orderedAt     DateTime @map("ordered_at") @db.Timestamptz
  paidAt        DateTime? @map("paid_at") @db.Timestamptz
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt     DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz
  company       Company          @relation(fields: [companyId], references: [id], onDelete: Cascade)
  orderItems    CoupangOrderItem[]
  @@index([companyId])
  @@index([status])
  @@index([orderedAt])
  @@map("coupang_orders")
}
```

From 01-RESEARCH.md Pattern 2 (CoupangReturn):
```prisma
model CoupangReturn {
  id            String   @id @default(uuid()) @db.Uuid
  companyId     String   @map("company_id") @db.Uuid
  receiptId     String   @unique @map("receipt_id") @db.VarChar(30)
  orderId       String   @map("order_id") @db.VarChar(30)
  requesterName String   @default("") @map("requester_name")
  receiptStatus String   @default("UC") @map("receipt_status")
  receiptType   String   @default("RETURN") @map("receipt_type")
  faultByType   String   @default("CUSTOMER") @map("fault_by_type")
  cancelReason  String   @default("") @map("cancel_reason")
  cancelReasonCategory1 String? @map("cancel_reason_category1")
  cancelReasonCategory2 String? @map("cancel_reason_category2")
  reasonCode    String?  @map("reason_code")
  reasonCodeText String? @map("reason_code_text")
  returnDeliveryId String? @map("return_delivery_id") @db.VarChar(30)
  enclosePrice  Int?     @map("enclose_price")
  requestedAt   DateTime @map("requested_at") @db.Timestamptz
  completedAt   DateTime? @map("completed_at") @db.Timestamptz
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt     DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz
  company       Company  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  returnItems   CoupangReturnItem[]
  @@index([companyId])
  @@index([receiptStatus])
  @@index([faultByType])
  @@index([requestedAt])
  @@map("coupang_returns")
}
```

From 01-RESEARCH.md Pattern 3 (ProductItem + Product 확장):
```prisma
// Product에 추가할 필드:
deliveryChargeType String? @map("delivery_charge_type")
freeShipOverAmount Int?    @map("free_ship_over_amount")
returnCharge       Int?    @map("return_charge")
deliveryInfo       Json?   @map("delivery_info")
images             Json?   @default("[]")
productItems       ProductItem[]

model ProductItem {
  id            String   @id @default(uuid()) @db.Uuid
  productId     String   @map("product_id") @db.Uuid
  vendorItemId  String?  @map("vendor_item_id") @db.VarChar(30)
  itemName      String   @default("") @map("item_name")
  originalPrice Int      @default(0) @map("original_price")
  salePrice     Int      @default(0) @map("sale_price")
  supplyPrice   Int      @default(0) @map("supply_price")
  isActive      Boolean  @default(true) @map("is_active")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz
  updatedAt     DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz
  product       Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  @@index([productId])
  @@map("product_items")
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Prisma 스키마에 CoupangOrder, CoupangReturn, ProductItem 모델 추가 및 Product 확장</name>
  <files>prisma/schema.prisma, package.json</files>
  <read_first>
    - prisma/schema.prisma (현재 전체 스키마 — Company 모델의 relation 배열 위치, Product 모델 구조 확인)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1~3 스키마 설계, offset 160~330)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01~D-10 locked decisions)
    - package.json (현재 scripts 섹션)
  </read_first>
  <action>
1. **prisma/schema.prisma** 수정:

   a) Company 모델의 relations 배열에 3개 추가:
   ```
   coupangOrders      CoupangOrder[]
   coupangReturns     CoupangReturn[]
   ```
   (ProductItem은 Company 직접 관계 없음 — Product를 통해 접근)

   b) Product 모델에 필드 5개 + relation 1개 추가 (기존 필드 아래, relations 위에):
   ```prisma
   deliveryChargeType String?  @map("delivery_charge_type")
   freeShipOverAmount Int?     @map("free_ship_over_amount")
   returnCharge       Int?     @map("return_charge")
   deliveryInfo       Json?    @map("delivery_info")
   images             Json?    @default("[]")
   ```
   Product 모델 relations 섹션에 추가:
   ```prisma
   productItems       ProductItem[]
   ```

   c) `// --- Orders ---` 섹션의 기존 `Order` 모델은 유지 (D-15~D-18에 따라 Plan 02에서 처리). 기존 Order 모델과 Company/Product의 관계도 유지.

   d) 새로운 섹션 `// --- Coupang Orders ---` 추가 (기존 Order 모델 아래):
   - `CoupangOrder` 모델: interfaces 블록의 Pattern 1 그대로 구현
   - `CoupangOrderItem` 모델: interfaces 블록의 Pattern 1 그대로 구현

   e) 새로운 섹션 `// --- Coupang Returns ---` 추가:
   - `CoupangReturn` 모델: interfaces 블록의 Pattern 2 그대로 구현
   - `CoupangReturnItem` 모델: interfaces 블록의 Pattern 2 그대로 구현

   f) 새로운 섹션 `// --- Product Items ---` 추가 (Product 섹션 바로 아래):
   - `ProductItem` 모델: interfaces 블록의 Pattern 3 그대로 구현

   **핵심 규칙 (per CLAUDE.md Prisma 규칙):**
   - UUID PK: `@id @default(uuid()) @db.Uuid`
   - camelCase 필드 → `@map("snake_case")` 컬럼
   - PascalCase 모델 → `@@map("snake_case")` 테이블
   - Native PG enum 금지 → 모든 status/type 필드는 String
   - 타임스탬프: `@db.Timestamptz`
   - 쿠팡 대형 ID: `String @db.VarChar(30)` (per D-07)
   - 금액: `Int` (KRW)

2. **package.json** 수정:
   scripts 섹션에 추가 (per D-14):
   ```json
   "db:seed-coupang": "tsx prisma/seed-coupang.ts"
   ```

3. **DB 적용 및 클라이언트 생성:**
   ```bash
   npm run db:push
   npx prisma generate
   ```
   db:push 시 Order 테이블 변경 경고가 나올 수 있으나 기존 Order 모델을 유지하므로 문제 없음.
  </action>
  <verify>
    <automated>cd /Users/yhc125/workspace/kiditem && npx prisma validate && npx prisma generate</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains "model CoupangOrder {"
    - prisma/schema.prisma contains "model CoupangOrderItem {"
    - prisma/schema.prisma contains "model CoupangReturn {"
    - prisma/schema.prisma contains "model CoupangReturnItem {"
    - prisma/schema.prisma contains "model ProductItem {"
    - prisma/schema.prisma contains "shipmentBoxId" with "@db.VarChar(30)"
    - prisma/schema.prisma contains "receiptId" with "@db.VarChar(30)"
    - prisma/schema.prisma contains "deliveryInfo" with "Json?"
    - prisma/schema.prisma contains "images" with "Json?"
    - Product model contains "productItems" relation
    - Company model contains "coupangOrders" relation
    - Company model contains "coupangReturns" relation
    - package.json contains "db:seed-coupang"
    - `npx prisma validate` exits 0
    - `npx prisma generate` exits 0
  </acceptance_criteria>
  <done>CoupangOrder, CoupangOrderItem, CoupangReturn, CoupangReturnItem, ProductItem 모델이 schema.prisma에 존재하고 prisma validate/generate가 통과한다. db:seed-coupang npm script가 package.json에 존재한다.</done>
</task>

</tasks>

<verification>
```bash
# 스키마 유효성
npx prisma validate

# 모델 존재 확인
grep -c "model CoupangOrder " prisma/schema.prisma    # 1
grep -c "model CoupangOrderItem " prisma/schema.prisma # 1
grep -c "model CoupangReturn " prisma/schema.prisma    # 1
grep -c "model CoupangReturnItem " prisma/schema.prisma # 1
grep -c "model ProductItem " prisma/schema.prisma      # 1

# VarChar(30) ID 타입 확인
grep "VarChar(30)" prisma/schema.prisma | wc -l  # 8개 이상

# npm script 확인
grep "seed-coupang" package.json  # 존재
```
</verification>

<success_criteria>
1. prisma/schema.prisma에 5개 신규/확장 모델이 존재하고 prisma validate가 통과한다
2. 모든 쿠팡 대형 ID 필드가 String @db.VarChar(30) 타입이다
3. package.json에 db:seed-coupang script가 존재한다
4. npx prisma generate가 성공하여 Prisma Client에 새 모델 타입이 생성된다
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
