---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - prisma/seed-coupang.ts
autonomous: true
requirements:
  - IMPT-01
  - IMPT-02
  - IMPT-03
  - IMPT-04
  - IMPT-05

must_haves:
  truths:
    - "npm run db:seed-coupang 실행 후 coupang_orders 테이블에 298건이 존재한다"
    - "npm run db:seed-coupang 실행 후 coupang_returns 테이블에 20건이 존재한다"
    - "npm run db:seed-coupang 실행 후 product_items 테이블에 200건 이상의 ProductItem이 존재한다"
    - "npm run db:seed-coupang을 두 번 실행해도 레코드 수가 변하지 않는다 (멱등성)"
    - "DB에 저장된 orderedAt 시간이 UTC 기준으로 올바르게 변환되어 있다 (KST -9시간)"
  artifacts:
    - path: "prisma/seed-coupang.ts"
      provides: "쿠팡 JSON 데이터 임포트 스크립트"
      contains: "parseKST"
      min_lines: 100
  key_links:
    - from: "prisma/seed-coupang.ts"
      to: "data/coupang_orders_raw.json"
      via: "readFile + JSON.parse"
      pattern: "coupang_orders_raw\\.json"
    - from: "prisma/seed-coupang.ts"
      to: "data/coupang_returns_all.json"
      via: "readFile + JSON.parse"
      pattern: "coupang_returns_all\\.json"
    - from: "prisma/seed-coupang.ts"
      to: "data/coupang_products_detail_50.json"
      via: "readFile + JSON.parse"
      pattern: "coupang_products_detail_50\\.json"
    - from: "prisma/seed-coupang.ts"
      to: "prisma.coupangOrder"
      via: "upsert"
      pattern: "prisma\\.coupangOrder\\.upsert"
---

<objective>
data/ 폴더의 쿠팡 JSON 파일(주문 298건, 반품 20건, 상품 상세 200건)을 DB에 멱등하게 임포트하는 seed-coupang.ts를 생성하고 실행한다.

Purpose: 실 데이터가 DB에 적재되어야 Phase 2(주문 대시보드), Phase 3(반품 대시보드), Phase 4(상품 상세)에서 조회할 수 있다.
Output: prisma/seed-coupang.ts 파일, DB에 적재된 298+20+200건 데이터
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

@prisma/schema.prisma
@prisma/seed.ts
</context>

<interfaces>
<!-- Research에서 확인된 데이터 파일 구조 + 임포트 패턴 -->

From 01-RESEARCH.md (Critical Data File Findings):

1. data/coupang_orders_raw.json:
   - 구조: JSON Array (NOT 객체)
   - 레코드 수: 298건
   - 각 레코드: { shipmentBoxId, orderId, orderer: {name, email, safeNumber}, receiver: {name, safeNumber, addr1, addr2, postCode}, status, orderItems: [{vendorItemId, vendorItemName, sellerProductId, sellerProductName, shippingCount, salesPrice, orderPrice, instantCouponDiscount}], deliveryCompanyName, invoiceNumber, orderedAt, paidAt, ... }
   - shipmentBoxId: 15자리 숫자
   - orderedAt: "2026-03-23T13:59:41" (KST naive)

2. data/coupang_returns_all.json:
   - 구조: JSON Array
   - 레코드 수: 20건
   - 각 레코드: { receiptId, orderId, requesterName, receiptStatus, receiptType, faultByType, cancelReason, cancelReasonCategory1, cancelReasonCategory2, reasonCode, reasonCodeText, returnDeliveryId, enclosePrice, returnItems: [{vendorItemId, vendorItemName, sellerProductId, sellerProductName, purchaseCount, cancelCount}], createdAt/completedAt }
   - returnDeliveryId: 19자리 숫자 (float64 정밀도 손실 이미 발생)
   - requestedAt: KST naive

3. data/coupang_products_detail_50.json + data/coupang_products_detail_150more.json:
   - 구조: JSON Array, 각 요소 { code, message, data: { sellerProductId, items: [{vendorItemId, itemName, originalPrice, salePrice, supplyPrice}], deliveryChargeType, freeShipOverAmount, returnCharge, deliveryInfo: {...}, images: [...] } }
   - 실제 데이터는 record.data 안에 있음! (Pitfall 6)
   - 50건 + 150건 = 200건

KST 파싱 헬퍼:
```typescript
function parseKST(naive: string | null | undefined): Date | null {
  if (!naive || naive === '') return null;
  return new Date(naive + '+09:00');
}
```

Prisma Client 초기화 패턴 (seed.ts와 동일):
```typescript
import { PrismaClient } from '@prisma/client';
import { PrismaPg } from '@prisma/adapter-pg';
const connectionString = process.env.DATABASE_URL!;
const adapter = new PrismaPg({ connectionString });
const prisma = new PrismaClient({ adapter });
```

파일 경로: seed-coupang.ts는 prisma/ 폴더에 위치하므로:
```typescript
const filePath = path.join(process.cwd(), 'data', 'coupang_orders_raw.json');
```
주의: process.cwd()는 프로젝트 루트(kiditem/)를 반환. prisma/ 폴더 기준이 아님.
단, worktree에서 실행 시 data/ 폴더가 없을 수 있으므로, 메인 레포 기준으로 경로 설정 필요.
실제 data/ 폴더 위치: /Users/yhc125/workspace/kiditem/data/ (메인 레포 루트)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: prisma/seed-coupang.ts 작성 — 주문/반품/상품 JSON 임포트 스크립트</name>
  <files>prisma/seed-coupang.ts</files>
  <read_first>
    - prisma/seed.ts (Prisma Client 초기화 패턴, PrismaPg adapter 사용법)
    - prisma/schema.prisma (CoupangOrder, CoupangReturn, ProductItem 모델 필드 정확한 이름 확인)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: parseKST, Pattern 5: 멱등 임포트, Code Examples: seed-coupang.ts 구조, offset 328~586)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11~D-14)
  </read_first>
  <action>
**prisma/seed-coupang.ts** 신규 생성 (per D-11):

1. **헤더/초기화:**
   ```typescript
   import { PrismaClient } from '@prisma/client';
   import { PrismaPg } from '@prisma/adapter-pg';
   import { readFile } from 'fs/promises';
   import path from 'path';

   const connectionString = process.env.DATABASE_URL!;
   const adapter = new PrismaPg({ connectionString });
   const prisma = new PrismaClient({ adapter });
   ```

2. **parseKST 헬퍼 (per D-08):**
   ```typescript
   function parseKST(naive: string | null | undefined): Date | null {
     if (!naive || naive === '') return null;
     return new Date(naive + '+09:00');
   }
   ```

3. **importOrders(companyId: string):**
   - 파일 읽기: `data/coupang_orders_raw.json` (Array 형태, Object.values() 불필요 — Research에서 수정됨)
   - 298건 순회, 각 shipmentBox에 대해:
     - `prisma.coupangOrder.upsert()` (per D-12, D-03):
       - `where: { shipmentBoxId: String(box.shipmentBoxId) }`
       - `update: { status: box.status, updatedAt: new Date() }`
       - `create`: 모든 CoupangOrder 필드 매핑
         - `companyId`
         - `shipmentBoxId: String(box.shipmentBoxId)` (per D-07)
         - `orderId: String(box.orderId)` (per D-07)
         - `orderer: box.orderer ?? null` (per D-02, Json)
         - `receiver: box.receiver ?? null` (per D-02, Json)
         - `status: box.status ?? 'ACCEPT'`
         - `deliveryCompanyName: box.deliveryCompanyName ?? null`
         - `invoiceNumber: box.invoiceNumber ? String(box.invoiceNumber) : null`
         - `parcelPrintMessage: box.parcelPrintMessage ?? null`
         - `shippingPrice: Number(box.shippingPrice ?? 0)`
         - `totalPrice: (box.orderItems ?? []).reduce((s, i) => s + (Number(i.orderPrice) || 0), 0)`
         - `orderedAt: parseKST(box.orderedAt)!` (per D-08)
         - `paidAt: parseKST(box.paidAt)` (per D-08)
     - `prisma.coupangOrderItem.deleteMany({ where: { orderId: order.id } })` (멱등성)
     - `prisma.coupangOrderItem.createMany()` for orderItems:
       - `orderId: order.id`
       - `vendorItemId: String(item.vendorItemId)` (per D-07)
       - `vendorItemName: String(item.vendorItemName ?? '')`
       - `sellerProductId: item.sellerProductId ? String(item.sellerProductId) : null`
       - `sellerProductName: String(item.sellerProductName ?? '')`
       - `shippingCount: Number(item.shippingCount ?? 1)`
       - `salesPrice: Number(item.salesPrice ?? 0)`
       - `orderPrice: Number(item.orderPrice ?? 0)`
       - `instantCouponDiscount: Number(item.instantCouponDiscount ?? 0)`
   - 로그: `console.log(\`Orders imported: ${raw.length}\`)`

4. **importReturns(companyId: string):**
   - 파일 읽기: `data/coupang_returns_all.json` (Array, 20건)
   - 20건 순회, 각 return에 대해:
     - `prisma.coupangReturn.upsert()` (per D-12, D-05):
       - `where: { receiptId: String(ret.receiptId) }`
       - `update: { receiptStatus: ret.receiptStatus ?? 'UC' }`
       - `create`: 모든 CoupangReturn 필드 매핑
         - `companyId`
         - `receiptId: String(ret.receiptId)` (per D-05)
         - `orderId: String(ret.orderId)` (per D-07)
         - `requesterName: String(ret.requesterName ?? '')`
         - `receiptStatus: ret.receiptStatus ?? 'UC'`
         - `receiptType: ret.receiptType ?? 'RETURN'`
         - `faultByType: ret.faultByType ?? 'CUSTOMER'` (per D-06)
         - `cancelReason: String(ret.cancelReason ?? '')`
         - `cancelReasonCategory1: ret.cancelReasonCategory1 ?? null`
         - `cancelReasonCategory2: ret.cancelReasonCategory2 ?? null`
         - `reasonCode: ret.reasonCode ?? null` (per D-06)
         - `reasonCodeText: ret.reasonCodeText ?? null` (per D-06)
         - `returnDeliveryId: ret.returnDeliveryId ? String(ret.returnDeliveryId) : null` (per D-07)
         - `enclosePrice: ret.enclosePrice ? Number(ret.enclosePrice) : null`
         - `requestedAt: parseKST(ret.createdAt)!` (per D-08 — 반품의 createdAt을 requestedAt으로 매핑)
         - `completedAt: parseKST(ret.completedAt)` (per D-08)
     - `prisma.coupangReturnItem.deleteMany({ where: { returnId: returnRecord.id } })`
     - `prisma.coupangReturnItem.createMany()` for returnItems:
       - `returnId: returnRecord.id`
       - `vendorItemId: item.vendorItemId ? String(item.vendorItemId) : null`
       - `vendorItemName: String(item.vendorItemName ?? '')`
       - `sellerProductId: item.sellerProductId ? String(item.sellerProductId) : null`
       - `sellerProductName: String(item.sellerProductName ?? '')`
       - `purchaseCount: Number(item.purchaseCount ?? 1)`
       - `cancelCount: Number(item.cancelCount ?? 1)`
   - 로그: `console.log(\`Returns imported: ${raw.length}\`)`

5. **importProductDetails():**
   - 두 파일 읽기: `data/coupang_products_detail_50.json` + `data/coupang_products_detail_150more.json`
   - 합치기: `const allDetails = [...detail50, ...detail150]` (200건)
   - 각 record에서 `const d = record.data` (Pitfall 6: .data 안에 실제 데이터)
   - sellerProductId로 기존 Product 찾기: `prisma.product.findFirst({ where: { coupangProductId: String(d.sellerProductId) } })`
   - Product가 있으면 update:
     - `deliveryChargeType: d.deliveryChargeType ?? null`
     - `freeShipOverAmount: d.freeShipOverAmount ? Number(d.freeShipOverAmount) : null`
     - `returnCharge: d.returnCharge ? Number(d.returnCharge) : null`
     - `deliveryInfo: d.deliveryInfo ?? null` (per D-09, Json 전체 저장)
     - `images: d.images ?? []` (per D-09, 이미지 URL 배열)
   - ProductItem 처리 (per D-10):
     - Product가 있으면 items 배열에 대해:
       - `prisma.productItem.deleteMany({ where: { productId: product.id } })`
       - `prisma.productItem.createMany()`:
         - `productId: product.id`
         - `vendorItemId: item.vendorItemId ? String(item.vendorItemId) : null`
         - `itemName: String(item.itemName ?? '')`
         - `originalPrice: Number(item.originalPrice ?? 0)`
         - `salePrice: Number(item.salePrice ?? 0)`
         - `supplyPrice: Number(item.supplyPrice ?? 0)`
   - Product가 없으면 skip (상품 먼저 seed되어야 함) + 카운터 증가
   - 로그: `console.log(\`Product details updated: ${updated}, skipped: ${skipped}\`)`

6. **main() 함수:**
   ```typescript
   async function main() {
     console.log('Importing Coupang data...');

     // 거영 company 조회 (seed.ts에서 이미 생성됨)
     const geoyoung = await prisma.company.findFirst({ where: { slug: 'geoyoung' } });
     if (!geoyoung) {
       throw new Error('Company "geoyoung" not found. Run npm run db:seed first.');
     }

     await importOrders(geoyoung.id);
     await importReturns(geoyoung.id);
     await importProductDetails();

     console.log('Coupang data import complete!');
   }

   main()
     .catch((e) => { console.error('Import error:', e); process.exit(1); })
     .finally(async () => { await prisma.$disconnect(); });
   ```

**파일 경로 주의:**
- `process.cwd()`는 npm script 실행 시 프로젝트 루트 반환
- 따라서 `path.join(process.cwd(), 'data', 'coupang_orders_raw.json')` 사용
- worktree에서 실행 시 data/ 폴더가 없을 수 있음 — 메인 레포에서 실행하거나 심링크 필요. 스크립트에 파일 존재 체크 추가: `if (!existsSync(filePath)) { console.warn(\`File not found: ${filePath}, skipping...\`); return; }`

**에러 핸들링:**
- 개별 레코드 실패 시 에러 로그 출력하고 continue (skip 패턴)
- 전체 파일 없으면 해당 임포트 건너뛰기
  </action>
  <verify>
    <automated>cd /Users/yhc125/workspace/kiditem && npx tsc --noEmit prisma/seed-coupang.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - prisma/seed-coupang.ts file exists with at least 100 lines
    - prisma/seed-coupang.ts contains "function parseKST"
    - prisma/seed-coupang.ts contains "prisma.coupangOrder.upsert"
    - prisma/seed-coupang.ts contains "prisma.coupangReturn.upsert"
    - prisma/seed-coupang.ts contains "prisma.productItem.deleteMany"
    - prisma/seed-coupang.ts contains "prisma.productItem.createMany"
    - prisma/seed-coupang.ts contains "coupang_orders_raw.json"
    - prisma/seed-coupang.ts contains "coupang_returns_all.json"
    - prisma/seed-coupang.ts contains "coupang_products_detail_50.json"
    - prisma/seed-coupang.ts contains "coupang_products_detail_150more.json"
    - prisma/seed-coupang.ts contains "record.data" (Pitfall 6 방지)
    - prisma/seed-coupang.ts contains "+09:00" (KST 변환)
    - prisma/seed-coupang.ts contains "String(" for ID conversion
    - prisma/seed-coupang.ts does NOT contain "Object.values(" (D-13 수정사항)
    - tsc compilation of seed-coupang.ts exits without errors
  </acceptance_criteria>
  <done>seed-coupang.ts가 존재하고, tsc 컴파일이 통과하며, parseKST 헬퍼와 upsert 패턴으로 3가지 데이터 소스(주문/반품/상품 상세)를 멱등하게 임포트하는 코드가 완성되어 있다</done>
</task>

<task type="auto">
  <name>Task 2: seed-coupang.ts 실행 및 DB 데이터 검증</name>
  <files>prisma/seed-coupang.ts</files>
  <read_first>
    - prisma/seed-coupang.ts (Task 1에서 생성한 파일)
    - package.json (db:seed-coupang script 확인)
  </read_first>
  <action>
1. **DB 적용 확인:**
   ```bash
   npm run db:push
   ```

2. **기존 시드 실행 (Company/Product 등 필요):**
   ```bash
   npm run db:seed
   ```

3. **쿠팡 데이터 임포트 실행:**
   ```bash
   npm run db:seed-coupang
   ```

4. **검증 쿼리 실행:**
   ```bash
   # 주문 건수 확인 (298건)
   npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM coupang_orders;"

   # 반품 건수 확인 (20건)
   npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM coupang_returns;"

   # 주문 아이템 건수 확인 (381건 예상)
   npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM coupang_order_items;"

   # 반품 아이템 건수 확인
   npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM coupang_return_items;"

   # 상품 아이템 건수 확인
   npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM product_items;"

   # 멱등성 검증: 두 번째 실행
   npm run db:seed-coupang

   # 두 번째 실행 후 건수 동일 확인
   npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM coupang_orders;"
   # 여전히 298이어야 함

   # KST 변환 검증: orderedAt이 UTC로 저장되었는지 확인
   npx prisma db execute --stdin <<< "SELECT shipment_box_id, ordered_at FROM coupang_orders LIMIT 3;"

   # 쿠팡 ID 직렬화 확인: shipmentBoxId가 문자열로 저장
   npx prisma db execute --stdin <<< "SELECT shipment_box_id, order_id FROM coupang_orders LIMIT 3;"
   ```

5. **에러 발생 시:**
   - 타입 에러: seed-coupang.ts의 필드 매핑 수정
   - 파일 not found: 경로 확인 (process.cwd() + 'data/')
   - DB 연결 에러: Docker compose up -d 확인, DATABASE_URL 확인
  </action>
  <verify>
    <automated>cd /Users/yhc125/workspace/kiditem && npx prisma db execute --stdin <<< "SELECT 'orders=' || (SELECT COUNT(*) FROM coupang_orders) || ', returns=' || (SELECT COUNT(*) FROM coupang_returns);"</automated>
  </verify>
  <acceptance_criteria>
    - SELECT COUNT(*) FROM coupang_orders returns 298
    - SELECT COUNT(*) FROM coupang_returns returns 20
    - SELECT COUNT(*) FROM coupang_order_items returns value > 0
    - SELECT COUNT(*) FROM coupang_return_items returns value > 0
    - Running npm run db:seed-coupang twice produces same counts (idempotent)
    - SELECT ordered_at FROM coupang_orders LIMIT 1 returns a UTC timestamp (not KST)
    - SELECT shipment_box_id FROM coupang_orders LIMIT 1 returns a string value (not BigInt)
  </acceptance_criteria>
  <done>coupang_orders에 298건, coupang_returns에 20건이 존재하고, 두 번 실행해도 건수가 변하지 않으며, 쿠팡 ID가 문자열로, 타임스탬프가 UTC로 저장되어 있다</done>
</task>

</tasks>

<verification>
```bash
# 1. 전체 데이터 건수
npx prisma db execute --stdin <<< "SELECT COUNT(*) as order_count FROM coupang_orders;"       # 298
npx prisma db execute --stdin <<< "SELECT COUNT(*) as return_count FROM coupang_returns;"      # 20
npx prisma db execute --stdin <<< "SELECT COUNT(*) as item_count FROM coupang_order_items;"    # 381+
npx prisma db execute --stdin <<< "SELECT COUNT(*) as return_item_count FROM coupang_return_items;"  # 20+

# 2. 멱등성 (두 번째 실행)
npm run db:seed-coupang
npx prisma db execute --stdin <<< "SELECT COUNT(*) FROM coupang_orders;"  # 여전히 298

# 3. KST→UTC 변환 검증
npx prisma db execute --stdin <<< "SELECT ordered_at AT TIME ZONE 'Asia/Seoul' as kst_time FROM coupang_orders LIMIT 1;"

# 4. String ID 직렬화 검증
npx prisma db execute --stdin <<< "SELECT pg_typeof(shipment_box_id), shipment_box_id FROM coupang_orders LIMIT 1;"
```
</verification>

<success_criteria>
1. coupang_orders 테이블에 298건 존재
2. coupang_returns 테이블에 20건 존재
3. product_items 테이블에 ProductItem 데이터 존재 (매칭된 Product 수만큼)
4. 임포트 스크립트를 두 번 실행해도 레코드 수가 변하지 않는다 (멱등성)
5. orderedAt 타임스탬프가 UTC로 올바르게 변환되어 저장되어 있다
6. shipmentBoxId 등 쿠팡 ID가 문자열로 저장되어 있다
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
