/**
 * Temporary source-compatibility export while retired family consumers are
 * removed in this reconstruction. It deliberately carries no predicate:
 * final MasterProduct rows are Sellpia identities and must not be classified
 * as a separate family subtype.
 */
export const LEGACY_FAMILY_MASTER_SCOPE = {} as const;
