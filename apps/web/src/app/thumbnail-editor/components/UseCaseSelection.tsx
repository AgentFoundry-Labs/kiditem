/**
 * Backend prompt routing keys shared by the hub, edit workspace, and modal controls.
 * The historical UseCaseSelection component was removed; this file remains as the
 * narrow type boundary used by the existing imports.
 */
export type EditUseCase = 'compose' | 'color-variants' | 'single' | 'bundle';
