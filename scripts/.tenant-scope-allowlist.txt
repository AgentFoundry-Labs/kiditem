# Tenant-scope scanner allowlist (scripts/check-tenant-scope.sh)
#
# Each non-comment line: <relative-path-from-repo-root> <pattern-id>
#   pattern-id ∈ { findUnique | idMutation | controllerParam | dtoField | all }
#   When omitted, defaults to "all" (exempt from every pattern).
#
# Allowlist entries should be narrow: one path + one pattern + a recorded reason.
# Adding "all" or broad globs defeats the gate. Re-justify when allowlisting.

# Marketplace is a globally-shared catalog (Marketplace model has no organizationId
# column — workflow/agent template rows are not tenant-owned). findUnique by
# bare id on this table is intentional. Tenant scope is enforced separately on
# the per-organization install records (WorkflowTemplate / AgentDefinition).
apps/server/src/marketplace/marketplace.service.ts findUnique

# Auth bootstrap exception: Supabase `payload.sub` identifies the current
# principal before organization context is selected. This is not a tenant-owned
# business resource read; the active OrganizationMembership selected here
# becomes the request organization context that downstream guards enforce.
apps/server/src/auth/middleware/supabase-auth.middleware.ts findUnique

# Auth self profile exception: `/api/auth/me` re-reads the already-authenticated
# principal from `@CurrentUser()` and is intentionally available to users without
# an active organization membership.
apps/server/src/auth/auth.controller.ts findUnique

# Agent OS v2 global catalog tables (`agent_tool_definitions`,
# `agent_blueprints`, `agent_blueprint_tool_policies`) have no `organization_id`
# column — they are the global catalog side of the schema in
# `prisma/models/agents.prisma`. Per-organization scoping happens on
# `agent_instances` and `agent_instance_tool_policies`, which the same adapter
# already accesses via `findFirst({ where: { id, organizationId } })`.
apps/server/src/agent-os/adapter/out/repository/agent-os.repository.adapter.ts findUnique
